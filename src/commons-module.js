// file in the parent directory will be the first one in the grunt concat order
angular.module('commons', []);

// override this for correct template URLs
var ANGULAR_COMMONS_TEMPLATES_PATH = '../build/templates/';